export interface IMypageTitle {
  isSuccess: boolean;
  title: string;
}

export interface IMypagePost {
  id: string;
  imageUrl: string;
  orderStatus: string;
  region: string;
  title: string;
  visitTime: string;
}
